import 'package:flutter/material.dart';

class MusicUploadingScreen extends StatelessWidget {
  const MusicUploadingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Song Upload Page',
        ),
      ),
    );
  }
}
